<%
   @title = "Edit Event"
   @title_link = edit_event_path(@event)
%>

<%= bootstrap_form_for @event do |f| %>
  <%= form_errors_for @event %>
  <%= render partial: 'helpers/event_fields', locals: { f: f } %>
  <br />
  <%= f.submit "Update" %>
<% end %>